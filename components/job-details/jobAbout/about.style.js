import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        padding: 20,
        backgroundColor: "#FFF",
        borderRadius: 12,
    },
    title: {
        fontSize: 20,
        fontWeight: "600",
        marginBottom: 20,
        color: "#312651"
    },
    body: {
        color: "#83829A"
    }
});

export default styles;